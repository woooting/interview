# 文档编写规范

> 本规范基于现有知识库文档风格提炼，所有新增内容必须遵守。

---

## 标题层级

| 级别 | 标记 | 用途 | 示例 |
|------|------|------|------|
| **H1** | `#` | 独立的知识领域，一个完整的"话题" | `# JavaScript事件循环` |
| **H2** | `##` | 大话题下的子主题 | `## 核心数据结构` |
| **H3** | `###` | 子主题下的具体细分 | `### 执行栈（Call Stack）` |
| **H4** | `####` | 最细粒度，仅在必要时使用 | `#### 工作机制` |

**规则：**
- H1 之间用 `---` 分隔
- 同一个 H1 下最多 3 层嵌套（H1 → H2 → H3 → H4），不再深
- 标题是陈述句或名词短语，不要用问句

---

## 各内容类型的写法

### 对比类（A vs B）

**先表格，后展开：**

```
| 维度 | A | B |
|------|---|---|
| 维度1 | xxx | xxx |
| 维度2 | xxx | xxx |
```

表格覆盖完核心差异后，每个维度再用 H3 单独展开详解。

### 流程类（步骤 1→2→3）

**用 ASCII 流程图或有序列表：**

```
1. 步骤一
2. 步骤二
3. 步骤三
```

或：

```
阶段一 → 阶段二 → 阶段三
  ↓
结果
```

### 定义+特性

一句话定义，特性用无序列表：

```
**XXX 是** 一句话说清楚它是什么。

- 特性一
- 特性二
- 特性三
```

### 原理剖析

从问题出发 → 核心机制 → 示例 → 总结：

```
**问题：为什么 X 会 Y？**

核心原因是 ...

```js
// 示例代码
```

因此，XXX 的实质是 YYY。
```

---

## 代码块

- **始终标注语言**：`js`、`javascript`、`bash`、`css`、`html` 等
- **不标注语言**的代码块只用于 ASCII 图表（如流程图、数据结构图示）
- 关键行用行内注释标注核心逻辑
- 示例代码保持简洁，不超过 30 行为佳

```
```js
const result = fn()  // 这里触发依赖收集
```

---

## 列表

### 有序列表

用于有先后顺序或优先级的内容：
- 操作步骤
- 优先级排序
- 演化历程（如版本演进）

### 无序列表

用于同等级别的特征罗列：
- 特性列表
- 分类罗列
- 多个并列要点

**规则：** 列表项如果内容很长（超过一行），用句号结尾；如果是短语，不用标点。

---

## 表格

- 始终有表头
- 对比类表格：左列是维度名，右列是内容
- 属性罗列表格：左列属性名，右列说明

| 属性值 | 作用 | 典型场景 |
|--------|------|----------|
| xxx    | xxx  | xxx      |

---

## 强调与标点

| 元素 | 用法 | 
|------|------|
| **加粗** | 关键术语首次出现、核心结论 |
| `行内代码` | 函数名、变量名、HTML 标签、CSS 属性值、文件名 |
| `> 引用块` | 黄金法则、关键警告、一句话总结 |
| `---` 分隔线 | 两个 H1 大主题之间 |

---

## 禁止事项

- 禁止 Q/A 对话格式（"问：xxx / 答：xxx"）
- 禁止 emoji
- 禁止空 Überschrift（有标题无内容）
- 禁止同一内容在多个文件中重复（用交叉引用 `> 另见：xxx/xxx.md`）
- 禁止超过 4 级标题嵌套
- 禁止代码块不标注语言（ASCII 图表除外）
