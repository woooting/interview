# 文档编写规范

> 本规范基于现有知识库文档风格提炼，所有新增内容必须遵守。

---

## 必须

| 类别 | 规则 |
|------|------|
| 标题 | 标题层次最多为两层，只能使用 H1 和 H2。H1 之间用 `---` 分隔。标题是陈述句或名词短语，不用问句 |
| 细分方式 | H2 内容如需进一步分点，必须使用有序列表或无序列表，不得使用加粗文字或行内代码模拟标题层级 |
| 内容结构 | 对比类：先表格后展开；流程类：用 ASCII 流程图或有序列表；定义+特性：一句话定义加无序列表列举特性；原理剖析：问题→核心机制→示例→总结 |
| 代码块 | 始终标注语言（`js`、`bash`、`css` 等），不标注语言的代码块只用于 ASCII 图表 |
| 列表 | 列表项超过一行用句号结尾；短语不用标点 |
| 表格 | 始终有表头。对比类：左列维度、右列内容；属性罗列：左列属性名、右列说明 |
| 加粗 | 关键术语首次出现、核心结论 |
| `行内代码` | 函数名、变量名、HTML 标签、CSS 属性值、文件名 |
| `> 引用块` | 黄金法则、关键警告、一句话总结 |

## 禁止

- 禁止 Q/A 对话格式（"问：xxx / 答：xxx"）
- 禁止 emoji
- 禁止空标题（有标题无内容）
- 禁止同一内容在多个文件中重复（用交叉引用 `> 另见：xxx/xxx.md`）
- 禁止超过两级标题嵌套
- 禁止代码块不标注语言（ASCII 图表除外）
- 禁止使用加粗文字或行内代码模拟标题层级
